# API keys — at least one is required for the app to work.
# The backend holds these server-side so users don't need to supply their own.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Default model shown in the frontend on first load (optional).
# Examples: claude-sonnet-4-6, gpt-4o
DEFAULT_MODEL=
