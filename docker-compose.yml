services:
  frontend:
    build: ./frontend
    ports:
      - "8088:80"
    depends_on:
      - backend
    networks:
      - frontend-net

  backend:
    build: ./backend
    environment:
      - EXECUTOR_URL=http://executor:9000
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    depends_on:
      - executor
    networks:
      - frontend-net
      - backend-net

  executor:
    build: ./executor
    networks:
      - backend-net

networks:
  frontend-net:
  backend-net:
    internal: true
